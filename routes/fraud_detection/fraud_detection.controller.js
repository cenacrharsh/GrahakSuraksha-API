// const { getAllPlanets } = require("../../database/models/planets.model");

function fraudDetectionHandler(req, res) {
  return res.status(200).json();
}

module.exports = {
    fraudDetectionHandler,
};

